const baseFlex = "flex";

export const flexStart = `${baseFlex} justify-start`;

export const flexCenter = `${baseFlex} justify-center`;

export const flexCrossCenter = `${baseFlex} align-center`;

export const flexBetween = `${baseFlex} justify-between`;

export const flexAround = `${baseFlex} justify-around`;

export const flexEvenly = `${baseFlex} justify-evenly`;

export const flexColumn = `${baseFlex} flex-col`;

export const flexRow = `${baseFlex} flex-row`;
